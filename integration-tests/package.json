{
  "name": "integration-tests",
  "version": "0.0.1",
  "scripts": {
    "start": "cypress open --browser chrome",
    "test": "cypress run --browser chrome"
  },
  "devDependencies": {
    "cypress": "^9.2.1"
  }
}
